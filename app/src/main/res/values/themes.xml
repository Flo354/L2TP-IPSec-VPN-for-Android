<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        The Activity is pure Compose; this theme only has to keep the window from flashing white
        before the first frame, so its background matches the Compose light palette.
    -->
    <style name="Theme.L2tpVpn" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
